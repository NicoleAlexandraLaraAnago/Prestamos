---
import MainLayout from '../../../layouts/MainLayout.astro';
import * as studentService from "../../services/studentService";
const { id } = Astro.params;
const student = await studentService.getEstudianteById(id)
---
  <MainLayout>
      <div class="bg-white shadow-md rounded-md p-4 mb-4">
           <h2 class="text-2xl font-bold">{student.nombre} {student.apellido}</h2>
            <p class="text-gray-700">{student.email}</p>
           <p class="text-gray-700">Teléfono: {student.telefono}</p>
         </div>
    </MainLayout>